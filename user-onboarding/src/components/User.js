import React from 'react'

export default function users(props){
    const details = (props)
    return (
        <>
    <h1>Welcome, {details.name}</h1>
    <h2>We have stored your email, {details.email}</h2>
    <h3>We hope you are happy in your new role, {details.role}</h3>
    </>

    )
}